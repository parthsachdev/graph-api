{
  "name": "graphs-api",
  "version": "1.0.0",
  "description": "This is a graph api written in Typescript inspired from the Algorithms (Part II) course in Coursera. The original api is written in Java, but I've converted that to Typescript for my purposes.",
  "main": "graph.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/typescript/bin/tsc",
    "start": "node ./output/index.js",
    "serve": "npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.9",
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1"
  }
}
